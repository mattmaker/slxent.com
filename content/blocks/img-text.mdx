export const NameOfBlock = ({ heading, text, image, alt}) => (
<Section py="8|12|16">
  <Container>
    <Grid col="1|2" gap="4|8">
      <Image src={image} alt={alt} />
      <Div>
        <H2 textAlign="center">{heading}</H2>
        <P>
          {text}
        </P>
      </Div>
    </Grid>
  </Container>
</Section>
)